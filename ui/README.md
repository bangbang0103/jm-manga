# JM Manga Flutter Client

Flutter client for JM Manga. It supports Android, iOS, desktop Flutter targets, and Flutter Web/PWA builds.

Common commands:

```bash
flutter pub get
flutter analyze --fatal-infos
flutter test
flutter run
```
