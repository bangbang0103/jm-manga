> 语言：[English](README.md) | 中文

# Flutter 客户端

JM Manga 的 Flutter 客户端。当前仅支持 Android 与 iOS。

常用命令：

```bash
flutter pub get
flutter analyze --fatal-infos
flutter test
flutter run
```

构建与发布脚本位于 `../scripts/release.sh`、`../scripts/build-flutter.sh` 和 `../scripts/package-server.sh`。
